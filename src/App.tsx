import type { Component } from 'solid-js';

const App: Component = () => {
  return (
    // We use `class` instead of the `className` used in React.
    <div>
      <h1>App component</h1>
    </div>
  );
};

export default App;
